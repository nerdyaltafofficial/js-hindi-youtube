// Arrays

const myArr = [0, 1, 2, 3, 4, 5];

// const myArr2 = new Array(1, 2, 3, 4, 5);

// const newArr=myArr.forEach((ele) => ele * 2)

// myArr.push(3,4);

// myArr.shift();
// myArr.shift();
// myArr.shift();
// myArr.shift();   Remove all elements from starting

// myArr.unshift(0, 1, 2, 3);

// console.log(myArr);

const nArr = [2, 3, 4, 5, 24, 34, 234];

nArr.splice(2, 2, "one", "two");

console.log(nArr);